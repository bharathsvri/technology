# Strict Mode

## `<StrictMode>`

Wrap app in development to enable extra checks: double effects, deprecated API warnings, unsafe lifecycles (classes).

## Purpose

Surface side-effect bugs early.

## Production

No runtime overhead in production build behavior beyond tree inclusion (StrictMode itself is stripped of dev behaviors).

Next: [Keys](21-Keys-and-Lists.md).
