# Portals and Refs

## `createPortal`

Render children into another DOM node (modals, tooltips escaping `overflow: hidden`).

## `forwardRef`

Pass ref through wrapper components to DOM or child APIs.

## Callback refs

`(el) => { ... }` when you need imperative attach/detach logic.

Next: [Performance](16-Performance-Optimization.md).
