# Composition Patterns

## `children` prop

Layout components wrap arbitrary subtree.

## Render props / slots

Pass `renderHeader={() => ...}` or component props for flexible APIs.

## Compound components

`Tabs`, `TabList`, `Tab` pattern with shared context.

## Prefer composition over inheritance

React favors composing small components vs deep OOP hierarchies.
