# Angular CLI

## Commands

- `ng new`, `ng generate component|service|pipe|directive|guard`
- `ng build`, `ng serve`, `ng test`, `ng lint`

## Configuration

`angular.json` — projects, build options, budgets, styles/scripts.

## Environments

Use `fileReplacements` or modern `define` patterns per configuration.

Next: [Lazy loading](23-Lazy-Loading-Routes.md).
