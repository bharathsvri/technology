# Template-Driven Forms

## `FormsModule`

`ngForm`, `ngModel`, validation directives (`required`, `minlength`).

## Two-way binding

`[(ngModel)]` on inputs tied to component fields.

## When to use

Simple forms, rapid prototyping; harder to unit-test than reactive forms.

Next: [Reactive forms](14-Reactive-Forms.md).
