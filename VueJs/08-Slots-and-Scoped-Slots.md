# Slots and Scoped Slots

## Default slot

`<slot />` where child content is projected.

## Named slots

`<slot name="header" />` with `#header` shorthand on parent.

## Scoped slots

`<slot :item="row" />` passes data to parent template for flexible rendering.

Next: [Provide / inject](09-Provide-and-Inject.md).
