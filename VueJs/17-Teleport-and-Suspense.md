# Teleport and Suspense

## `<Teleport to="body">`

Render modal subtree elsewhere in DOM to escape stacking contexts.

## `<Suspense>`

Async setup / async components with `#default` and `#fallback` slots.

Next: [Custom directives](18-Custom-Directives.md).
