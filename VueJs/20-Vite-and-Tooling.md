# Vite and Tooling

## Vite + Vue

Fast HMR, native ESM dev server, Rollup production build.

## `vite.config.ts`

Plugins: `@vitejs/plugin-vue`, path aliases, env variables (`import.meta.env`).

## Testing

**Vitest** + `@vue/test-utils` for unit tests.

## Lint/format

ESLint Vue plugin, Prettier.
