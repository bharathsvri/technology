# Single-File Components

## Sections

`<template>`, `<script setup lang="ts">`, `<style scoped>`.

## `scoped` CSS

Adds attribute selectors to limit style leakage.

## CSS modules

`<style module>` → `$style.className` object.

## Preprocessors

`lang="scss"` with Vite preprocessor installed.

Next: [Pinia](14-Pinia-State-Management.md).
